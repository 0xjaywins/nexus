"use client"; // Added: Mark as client component to avoid server-side prerendering

import { StakingPage } from "../../../components/staking/staking-page";

export default function Staking() {
  return <StakingPage />;
}
